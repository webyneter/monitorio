version: '3.6'

networks:
  grafana-frontend:
    external:
      name: reversario_default
  prometheus-frontend:
    external:
      name: reversario_default

services:
  grafana:
    networks:
      - grafana-frontend
      - backend

  prometheus:
    networks:
      - prometheus-frontend
      - backend
